# Analysis Data Manifest

The raw files in `data/raw/` contains datasets directly used by the cleaned analysis pipeline and original raw answers from GPT.

Raw file:
- `GPT_answer.xlsx`: Raw answers collected from 3 users. 

Retained inputs:

- `exploded_data_filtered2.csv`: across-user sentence-level data.
- `exploded_data_stages1.csv`: stage sentence-level data for user 1.
- `exploded_data_stages2.csv`: stage sentence-level data for user 2.
- `exploded_data_stages3.csv`: stage sentence-level data for user 3.

